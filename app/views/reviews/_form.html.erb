<%= simple_form_for([@post, @post.reviews.build]) do |f| %>
	<%= f.input :comment %>
	<%= f.button :submit, label: "Email",  :class => 'btn-custom2' %>
<% end %>

<script>
	$('#rating-form').raty({
		path: '/assets/',
		scoreName: 'review[rating]'
	});
</script>